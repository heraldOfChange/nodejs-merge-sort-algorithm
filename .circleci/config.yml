version: 2
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run:
          name: install node modules
          command: npm install
      - run:
          name: running tests
          command: npm run test
